<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="./main.css">
    <title>Users Main</title>
  </head>
  <body>
    <h1>All Users</h1>
    <div class="container">
      {{#data}}
      <div class="users">
        <img src="{{avatar}}" alt="Avatar Picture">
        <a href="/{{id}}">{{name}}</a>
        <h2>{{company}}</h2>
        <h3>{{job}}</h3>
      </div>
      {{/data}}
    </div>
  </body>
</html>
